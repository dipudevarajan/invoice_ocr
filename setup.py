from setuptools import setup, find_packages

setup(
    name="invoice_ocr",
    version="0.0.1",
    description="AI OCR Invoice Importer for ERPNext",
    author="Your Name",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
